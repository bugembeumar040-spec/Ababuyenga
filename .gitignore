# generation + render intermediates — reproducible via build/build.py + build/captions.py
build/raw/
build/seg/
build/out/
build/vo/
build/cap/
build/frames/
build/tagtest/

# delivery copy — re-encode from the master when needed
media/*_preview.mp4
